location:
  default: bedroom
  actions: [go]
  rules:
    entrance -- lawn: 
      - if not state: entrance.objects.door.door:open
      - then message: doorNotOpen
      - then revert: location
  entities:
    entrance:
      - objects.door
    lawn:
      - people.ada
