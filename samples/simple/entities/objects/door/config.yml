door:
  initial: closed
  actions: [open, close]
  rules:
    open -> closed: 
      if action close:
        message: doorAlreadyOpen
        state: open
    closed -> open:
      if action open:
        message: doorAlreadyClosed
        state: closed
