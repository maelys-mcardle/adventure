door:
  default: closed
  actions: [open, close]
  rules:
    - open -> closed: 
      - if action: close
      - set message: door_already_open
    - closed -> open:
      - if action: open
      - set message: door_already_closed
