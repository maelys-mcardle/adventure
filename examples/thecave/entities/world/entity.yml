location:
  value: shack
  actions: [describe, walk]
  entities:
    cave:
      - world.cave
    shack:
      - objects.torch
  rules:
    shack:
      when shack.objects.torch.state is possessed:
        for cave.world.cave.visible:
          value: true